# This YAML file describes your package. Stack will automatically generate a
# Cabal file when you run `stack build`. See the hpack website for help with
# this file: <https://github.com/sol/hpack>.
benchmarks:
  exp1-benchmarks:
    dependencies:
    - base
    - exp1
    - criterion
    ghc-options:
    - -rtsopts
    - -threaded
    - -with-rtsopts=-N
    main: Main.hs
    source-dirs: benchmark
category: Other
description: exp1 is a new Haskeleton package.
executables:
  exp1:
    dependencies:
    - base
    - exp1
    ghc-options:
    - -rtsopts
    - -threaded
    - -with-rtsopts=-N
    main: Main.hs
    source-dirs: executable
extra-source-files:
- CHANGELOG.md
- LICENSE.md
- package.yaml
- README.md
- stack.yaml
ghc-options: -Wall
github: githubuser/exp1
library:
  dependencies:
  - base
  source-dirs: library
license: MIT
maintainer: Author name here
name: exp1
synopsis: A new Haskeleton package.
tests:
  exp1-test-suite:
    dependencies:
    - base
    - exp1
    - tasty
    - tasty-hspec
    ghc-options:
    - -rtsopts
    - -threaded
    - -with-rtsopts=-N
    main: Main.hs
    source-dirs: test-suite
version: '0.0.0'
